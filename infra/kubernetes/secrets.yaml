apiVersion: v1
kind: Secret
metadata:
  name: systeme-sante-secrets
  namespace: default
  labels:
    app: systeme-sante
    component: secrets
type: Opaque
stringData:
  # PostgreSQL credentials
  postgres-user: "medical_user"
  postgres-password: "p$VMO<J;ePZ1RS:OpVxnQL%k"
  
  # MongoDB credentials
  mongo-root-username: "mongo_admin"
  mongo-root-password: "p$VMO<J;ePZ1RS:OpVxnQL%k"
  
  # MinIO credentials
  minio-root-user: "minio_admin"
  minio-root-password: "p$VMO<J;ePZ1RS:OpVxnQL%k"
  
  # JWT secrets (must be at least 32 bytes base64)
  jwt-secret: "LFPM+Q2c69KCHRPFi8c8tWw42eKuPWiMGF9IWD4lUtw="
  jwt-refresh-secret: "LFPM+Q2c69KCHRPFi8c8tWw42eKuPWiMGF9IWD4lUtw="
  token-encryption-key: "LFPM+Q2c69KCHRPFi8c8tWw42eKuPWiMGF9IWD4lUtw="
  
  # External API keys
  deepseek-api-key: "CHANGE_ME_DEEPSEEK_API_KEY"
  abdm-client-id: "CHANGE_ME_ABDM_CLIENT_ID"
  abdm-client-secret: "CHANGE_ME_ABDM_CLIENT_SECRET"
  whatsapp-business-access-token: "CHANGE_ME_WHATSAPP_ACCESS_TOKEN"
  whatsapp-business-phone-number-id: "CHANGE_ME_WHATSAPP_PHONE_ID"
  
  # Database URLs (with embedded secrets)
  database-url: "postgresql://medical_user:p$VMO<J;ePZ1RS:OpVxnQL%k@postgres-service:5432/medical_db?schema=public"
  mongo-url: "mongodb://mongo_admin:p$VMO<J;ePZ1RS:OpVxnQL%k@mongodb-service:27017/medical_db?authSource=admin"
  redis-url: "redis://redis-service:6379"
