--- apps/api/src/app/sync/sync.service.ts
+++ apps/api/src/app/sync/sync.service.ts
@@ -204,11 +204,13 @@
         });
       }

-      for (const id of changes.prescriptions.deleted) {
-        await this.prisma.prescription.update({
-          where: { id },
+      if (changes.prescriptions.deleted && changes.prescriptions.deleted.length > 0) {
+        await this.prisma.prescription.updateMany({
+          where: {
+            id: { in: changes.prescriptions.deleted }
+          },
           data: { deletedAt: new Date(), status: 'deleted' }
         });
       }
     }
