<<<<<<< SEARCH
      if (hasModifications) {
        record.data = dataObj;
        record.markModified('data');
        record.status = 'deleted';
        record.deletedAt = new Date();
        await record.save();
        updatedCount++;
      }
    }

    return updatedCount;
=======
      if (hasModifications) {
        record.data = dataObj;
        record.markModified('data');
        record.status = 'deleted';
        record.deletedAt = new Date();
        await record.save();
        updatedCount++;
      }
    }

    return updatedCount;
>>>>>>> REPLACE
