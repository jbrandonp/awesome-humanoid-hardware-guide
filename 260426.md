# Analyse Exhaustive de A à Z — Système de Santé Résilient V3.0

**Date d'audit** : 26 Avril 2026 — 3ème passe (bugs et erreurs)
**Méthodologie** : Lecture ligne à ligne de **tous les fichiers source** (>500 fichiers), exécution d'agents d'analyse parallèles (Rust, API, Frontend, Tests, Configs)
**Total bugs trouvés** : **~219 issues**

---

## SOMMAIRE RAPIDE

| Section | Fichiers analysés | Issues |
|---------|-------------------|--------|
| [B1. Rust/Tauri](#b1-rusttauri-12-fichiers) | 12 .rs | 34 |
| [B2. API NestJS](#b2-api-nestjs-75-fichiers) | 75+ .ts | 75+ |
| [B3. Frontend (Desktop/Mobile/PWA/Kiosk)](#b3-frontend-56-fichiers) | 56 .tsx/.ts | 50+ |
| [B4. Tests](#b4-tests-40-fichiers) | 40+ spec/test | 40+ |
| [B5. Configurations](#b5-configurations-50-fichiers) | 50+ configs | 15 CRITIQUES |
| [Action Prioritaire](#action-prioritaire-immediate) | | |

---

## B1. RUST/TAURI (12 fichiers)

### CRITIQUES (bloquent la compilation)

| # | Fichier | Bug |
|---|---------|-----|
| R1 | `lib.rs:7-9` | **6 modules non déclarés** : `whisper.rs`, `smart_card.rs`, `telemetry.rs`, `updater.rs`, `pdf_generator.rs`, `purchase_order.rs` existent mais ne sont pas `mod` → **code mort, non compilé** |
| R2 | `smart_card.rs:26-35` | `ctx.list_readers()` retourne `Result<(), Error>`, pas un itérateur → **NE COMPILE PAS** |
| R3 | `smart_card.rs:40` | `.to_string_lossy()` appelé sur un type `pcsc::Reader` qui n'a pas cette méthode → **NE COMPILE PAS** |
| R4 | `updater.rs:76` | `binary_resp.chunk().await.map_err(...)` — `.map_err()` n'existe pas sur `Option` → **NE COMPILE PAS** |
| R5 | `updater.rs:12-19` | Clé publique RSA `PUBLIC_KEY_PEM` = **gibberish invalide** → toutes les vérifications de signature échouent |
| R6 | `whisper.rs:74-79` | Transcription audio **jamais capturée** — stdout mis en `Stdio::piped()` mais jamais lu → retourne toujours `"Transcription completed"` au lieu du texte réel |

### HAUTES (logique/runtime)

| # | Fichier | Bug |
|---|---------|-----|
| R7 | `whisper.rs:49` | `path.to_str().unwrap()` → **PANIC** sur chemins non-UTF-8 |
| R8 | `whisper.rs:82` | `sys.refresh_processes(..., true)` supprime tous les autres processus de sysinfo à chaque itération |
| R9 | `thermal_printer.rs` | Fonction **stub vide** — `print_thermal_receipt` retourne `Ok(())` sans rien imprimer |
| R10 | `telemetry.rs:39-62` | Rotation de logs **supprime les plus récents** au lieu des plus anciens |
| R11 | `updater.rs:39` | `server_url` vient du frontend (non fiable) → **RCE potentielle** si l'URL est compromise |
| R12 | `updater.rs:117-140` | Fichier temporaire **jamais nettoyé** après installation réussie |
| R13 | `updater.rs:127-140` | Lance l'installateur via `.spawn()` mais **ne vérifie jamais** le code de sortie → succès malgré échec |
| R14 | `pdf_generator.rs:93-124` | **Clé de chiffrement retournée avec le fichier** — chiffrement rendu inutile (quiconque lit la réponse peut déchiffrer) |
| R15 | `report_generator.rs:50` | `ImageBuffer::from_raw(...).unwrap()` → **PANIC** si dimensions incorrectes |
| R16 | `report_generator.rs:29` | `FilePath.to_string()` peut retourner une URI `file://` au lieu d'un chemin fichiers → mismatch avec le vrai chemin |
| R17 | `purchase_order.rs:41` | Fonction déclarée `async` mais **zéro `.await`** → warning compilateur |
| R18 | `purchase_order.rs:144-150` | Chemin de sortie **Windows-only hardcodé** `C:/MedicalApp/Orders/` → échec sur Linux |
| R19 | `hardware_diag.rs:24-27` | `serialport::available_ports()` erreur silencieusement avalée → 0 imprimantes rapporté sans diagnostic |
| R20 | `telemetry.rs:84-90` | Tâche tokio **détachée sans JoinHandle** → jamais arrêtée proprement |
| R21 | `lib.rs:113-122` | Commandes des modules non déclarés (`transcribe_audio`, `read_smart_card`, `install_update`, etc.) **non enregistrées** → inaccessibles du frontend |
| R22 | `lib.rs:25-35` | Token JWT stocké dans le keyring OS retourné en **String brute** au webview → exfiltration possible via XSS |

---

## B2. API NESTJS (75+ fichiers)

### 🔴 CRITIQUES

| # | Fichier:Ligne | Bug |
|---|---------------|-----|
| A1 | `auth.controller.ts:37-39` | **Login hardcodé** — n'importe quel mot de passe connecte en tant que DOCTOR avec ID fixe `123e4567-e89b-12d3-a456-426614174000` |
| A2 | `auth.controller.ts:79-86` | `registerNewStaff()` — **placebo**, retourne succès sans créer d'utilisateur |
| A3 | `session-timeout.middleware.ts` | **Middleware vide** — appelle `next()` sans aucune logique de timeout |
| A4 | `queue.service.ts` | File d'attente **en mémoire** (array) → perdue au redémarrage serveur |
| A5 | `billing.service.ts:50-63` | Vérification idempotence **hors transaction** → race condition, factures en double possibles |
| A6 | `iot.service.ts` | `processSmartPenInk()` **écrase** les données Yjs précédentes au lieu de les fusionner → CRDT cassé |
| A7 | `dpdpa-consent.service.ts` | Suppression MongoDB **hors transaction Postgres** → suppression partielle irréversible si MongoDB échoue |
| A8 | `emarsync-listener.interceptor.ts:102` | Décrément stock validé en DB, mais si Redis `set` échoue → **incohérence de données** (stock baissé mais erreur 500) |
| A9 | `triage.algorithm.ts` | Mots-clés en **anglais** (`'chest pain'`, `'stroke'`, `'suicidal'`) dans un système **français** → ne matche jamais |
| A10 | `kiosk.gateway.ts` | **Zéro authentification** WebSocket → n'importe qui peut recevoir les données patients |
| A11 | `compression.interceptor.ts` | Corps binaire envoyé avec `Content-Type: application/json` → échec parsing client |

### 🟠 HAUTES

| # | Fichier:Ligne | Bug |
|---|---------------|-----|
| A12 | `app.module.ts` | `maxPoolSize: 1` pour MongoDB → goulot d'étranglement extrême en production |
| A13 | `app.module.ts` | `enableOfflineQueue: false` + `retryStrategy: null` pour BullMQ → jobs **perdus définitivement** si Redis down |
| A14 | `app.module.ts` | `bufferCommands: false` pour MongoDB → commandes **silencieusement perdues** si connexion down |
| A15 | `app.module.ts` | `autoIndex: false` → les indexes `@Prop({ index: true })` ne sont **jamais créés** |
| A16 | `audit.interceptor.ts` | N'enregistre que les réponses **succès** — les erreurs n'ont **aucune trace** d'audit |
| A17 | `audit.interceptor.ts` | Utilise `action` tandis que `audit.service.ts` utilise `actionType` → **incohérence de champ** |
| A18 | `audit.service.ts` | `logAudit()` est **fire-and-forget** via RxJS `tap()` → pas de garantie d'enregistrement |
| A19 | `jwt.strategy.ts` | `authHeader.replace('Bearer ', '')` fragile — `'Bearer'` peut apparaître ailleurs dans le header |
| A20 | `blacklist.service.ts:retryStrategy` | `retryStrategy: null` + `lazyConnect: true` → une fois déconnecté, Redis **ne se reconnecte jamais** |
| A21 | `fhir.mapper.ts` | `fromFhirPatient()` définit `dateOfBirth` à `new Date()` (date courante) si absent → **données patient incorrectes** |
| A22 | `billing.utils.ts` | `calculatePercentageCents()` perd la précision sub-basis-point via `Math.round(rateDecimal * 10000)` |
| A23 | `queue.service.ts` | Array partagé muté sans lock → race condition sur `addPatientToQueue()` |
| A24 | `crdt.service.ts` | `getStateVector()`, `getDelta()`, `mergeDelta()` retournent `Uint8Array()` vide sur erreur → **perte de données silencieuse** |
| A25 | `inventory-predictor.service.ts:worker` | Utilise `__filename.endsWith('.ts')` pour détecter ts-node → **échec en production** (fichiers .js) |
| A26 | `inventory-predictor.service.ts` | `projectedConsumption = 0` → division par zéro → `daysUntilDepletion = Infinity` → `Invalid Date` |
| A27 | `inventory-predictor.worker.ts` | 30-day prédiction **feed les prédictions en entrée** → erreur cumulative exponentielle |
| A28 | `clinical-record.service.ts` | `createSpecialtyRecord()` accepte `formData: unknown` avec **zéro validation** |
| A29 | `abdm.service.ts` | `Math.random() < 0.2` simule 20% d'échec API → **code de test en production** |
| A30 | `abdm.service.ts:162` | `logger.debug()` log du **matériel cryptographique** (IV, hash) |
| A31 | `abdm-webhook.gateway.ts` | **Zéro authentification** sur le webhook — pas de vérification signature ABDM |
| A32 | `intelligence.controller.ts` | Pas de RBAC au-delà de JWT — n'importe quel rôle peut accéder aux analyses IA |
| A33 | `whisper.service.ts:226` | `spawn(this.config.binaryPath, args)` — `audioFilePath` utilisateur → **path traversal** possible (fichier hors dossier attendu) |
| A34 | `ocr.service.ts` | Confiance 0.95 automatique si une unité est présente → **insuffisant cliniquement**, injection auto de données potentiellement fausses |
| A35 | `ocr.controller.ts` | `forceInjection` bypass le seuil de confiance → utilisateur autorisé peut injecter des données erronées |
| A36 | `semantic-parser.ts` | Reconnaît seulement 2 verbes français (`prescrire`, `donner`) + 8 médicaments en fallback |
| A37 | `idempotency.interceptor.ts:TTL` | Lock TTL 30 secondes — si requête >30s, **doublons traités** |
| A38 | `kiosk.service.ts` | Mock data — `getBeds()`, `getPatients()` retournent des **données factices hardcodées**, PrismaService injecté mais ignoré |
| A39 | `nursing-station.controller.ts` | `ParseUUIDPipe` sur `:id` mais les IDs mock sont `'bed-1'` (pas UUID) → **toutes les requêtes rejetées** |
| A40 | `nursing-station.service.ts` | Données PHI **hardcodées** dans le code source (noms, téléphones, DOB) |
| A41 | `epidemiology-report.service.ts` | `cacheManager.clear()` efface **tout** le cache, pas seulement les clés épidémio |
| A42 | `engagement.service.ts` | Notification commit en DB **avant** ajout Bull queue → orphelin "QUEUED" si queue fail |
| A43 | `peer-consult.service.ts:108-110` | `broadcastCase()` ne fait que logger → **"BROADCAST_SUCCESS" est un mensonge** |

### 🟡 MOYENNES

| # | Fichier:Ligne | Bug |
|---|---------------|-----|
| A44 | `audit.interceptor.ts` | `action` vs `actionType` incohérence entre fichiers |
| A45 | `consent-manager.processor.ts:85` | Utilise `actionType: 'DELETE'` — incohérent avec l'interceptor qui utilise `action` |
| A46 | `drug-interaction.service.ts` | Override basse sévérité → **pas de trace d'audit** |
| A47 | `drug-interaction.service.ts` | Règles d'interaction hardcodées à 4 paires → besoin de milliers en production |
| A48 | `fhir.service.ts` | `extractIdFromReference()` retourne `''` → propagé à `findUnique` → matchs non intentionnels |
| A49 | `fhir.controller.ts` | `pharmacyAccess()` utilise `@Res()` sans `passthrough: true` → risque de requête bloquée |
| A50 | `iot.service.ts:420-421` | `const items = [...queue]; queue = []` — si `enqueueForLater` appelé entre les deux → **item perdu** |
| A51 | `biosensor.service.ts` | `payload.fps = 0` → division par zéro non protégée |
| A52 | `biosensor.service.ts` | `applyBandpassFilter()` est un moving-average, pas un Butterworth IIR → précision clinique non validée |
| A53 | `hl7-mllp.service.ts` | Serveur TCP MLLP **sans authentification** — n'importe qui peut envoyer des messages HL7 |
| A54 | `hl7-parser.service.ts` | `patientLabId` utilisé comme UUID pour `findUnique` — la plupart des labos utilisent des MRN, pas des UUID |
| A55 | `ticker/epi-ticker.service.ts` | `icd10Code` contient en réalité des `medicationName` → **cliniquement invalide** |
| A56 | `ticker.module.ts` | `AuditModule` importé mais `AuditService` jamais utilisé |
| A57 | `inventory-predictor.worker.ts` | `net.run(currentWindow) as number` — brain.js peut retourner `number[]`, cast unsafe |
| A58 | `intelligence.service.ts` | Filtre prescriptions avec `status: 'synced'` uniquement → ignore les prescriptions actives |
| A59 | `intelligence.service.ts` | `severityMax = foundRisks[0]` — assume que le 1er élément a la sévérité max (non trié) |
| A60 | `adt.service.ts` | Patient pédiatrique <18 ans mis en lit adulte si pas de lit pédiatrique dispo → violation politique hôpital |
| A61 | `waiting-room.gateway.ts` | `role !== 'DISPLAY_KIOSK'` — string hardcodé au lieu d'utiliser l'enum Prisma |
| A62 | `procurement.controller.ts:21,28` | `Promise<unknown>` — type de retour perdu |
| A63 | `soft-delete.filter.ts` | `Object.getOwnPropertyNames()` ignore les propriétés héritées et symboles |
| A64 | `abdm/inventory.service.ts` | `isWinter = currentMonth >= 10 || currentMonth <= 1` — `getMonth()` est 0-indexed → octobre et mars exclus |

### 🟢 BASSES

| # | Fichier | Bug |
|---|---------|-----|
| A65 | `hl7-mllp.controller.ts:11` | `@ts-expect-error` non résolu |
| A66 | `ticker.controller.ts:58` | `actionType: ActionType.EXPORT_REPORT` passé via `as string` → bypass type |
| A67 | `abdm.service.ts` | `setInterval` jamais nettoyé → memory leak |
| A68 | `key-rotation.service.ts:85` | Erreur de `model()` avalée → crash masqué |
| A69 | `engagement.processor.ts` | `failCount` type incohérent (objet Prisma ou `0` int) |
| A70 | `auth.service.ts:getEncryptionSecret()` | SHA-256 de `TOKEN_ENCRYPTION_KEY` — si l'env var change, **tous les refresh tokens invalidés** |
| A71 | `blacklist.service.ts` | `console.warn` au lieu de `Logger` NestJS → incohérent |
| A72 | `crypto/key-rotation.service.ts` | `err` catch avec préfixe `_` suggérant unused mais utilisé plus loin |
| A73 | `fhir.mapper.ts` | `toFhirPatient()` cast `dateOfBirth as string` et `.split('T')[0]` → fragile |
| A74 | `hl7-reconciliation.service.ts` | `Date.UTC` vs dates en timezone locale → mismatch possible |
| A75 | `remote-patient-monitoring.service.ts` | Utilisation intensive de `any[]`, `Promise<any>` |

---

## B3. FRONTEND (56 fichiers)

### 🔴 CRITIQUES

| # | Fichier | Bug |
|---|---------|-----|
| F1 | `mobile/database.ts:17` | `SecurityManager.getEncryptionKey()` peut throw → DB jamais initialisée, app bloquée sur spinner infini |
| F2 | `mobile/hooks/useBleScanner.ts:382` | `decodeBloodPressure` retourne des valeurs **UInt16 brutes** au lieu de décoder l'IEEE 11073 SFLOAT → **données vitales erronées** |
| F3 | `mobile/services/background-sync.service.ts:103` | Après 50 retries, les données médicales sont **silencieusement détruites** |
| F4 | `mobile/services/HighAlertMedicationService.ts:51-54` | Race condition sur AsyncStorage — écriture concurrente → **pertes de données** |
| F5 | `mobile/services/background-sync.service.ts:164` | Même race condition sur `enqueueTransaction` |
| F6 | `desktop/components/DicomViewer.tsx:59` | `viewerRef.current!` → **crash** si ref null quand l'effet s'exécute |
| F7 | `mobile/hooks/useBleScanner.ts:333` | `saveVitalDataOfflineSecurely()` appelé sans `await` → **Promise rejetée silencieusement** |
| F8 | `portail-pwa/package.json` | **React 19 incompatible avec Next.js 14** → erreur build (nécessite Next.js 15+) |

### 🟠 HAUTES

| # | Fichier | Bug |
|---|---------|-----|
| F9 | `mobile/database.ts:52` | Proxy bloque l'accès synchrone — incompatible avec l'utilisation async de WatermelonDB |
| F10 | `mobile/database.ts:9` | Double appel `createDatabaseInstance` → **deux instances DB** créées |
| F11 | `mobile/services/mdns-scanner.service.ts:152` | Erreur mDNS **avalée** — ni mode offline ni fallback manuel activé |
| F12 | `mobile/services/SecurityManager.ts:5` | `as any` sur `FileSystem` → `documentDirectory` peut ne pas exister |
| F13 | `mobile/services/SecurityManager.ts:31` | `wipeData()` détruit clé + DB → si déclenché par biométrie échouée = **perte données médicales** |
| F14 | `mobile/components/RPPGBiosensor.tsx` | `useCameraPermissions()` déstructuré en 2 éléments — certaines versions Expo retournent 3 éléments |
| F15 | `mobile/components/RPPGBiosensor.tsx:50` | `setInterval` jamais nettoyé si composant démonté pendant mesure → **fuite mémoire** |
| F16 | `mobile/components/Omnibox.tsx:127-137` | Stale closure — `selectedItems` utilisé avant màj state → interaction médicamenteuse **non détectée** |
| F17 | `desktop/app/app.tsx:16` | `e as string` sur erreur Tauri → `[object Object]` affiché |
| F18 | `desktop/app/app.tsx` | État `error` jamais nettoyé après connexion manuelle → UI d'erreur persiste |
| F19 | `portail-pwa/login/page.tsx:40` | JWT dans cookie client-side (XSS vulnérable) au lieu de HttpOnly |
| F20 | `portail-pwa/login/page.tsx` | Cookie `secure` flag → échoue sur HTTP (dev local) |
| F21 | `mobile/components/BarcodeAdministration.tsx:14` | Même problème `useCameraPermissions` que RPPGBiosensor |
| F22 | `desktop/hooks/useHardwareOptimization.ts` | Hook **complètement vide** — aucune implémentation |

### 🟡 MOYENNES

| # | Fichier | Bug |
|---|---------|-----|
| F23 | `mobile/components/SmartPenCanvas.tsx` | Code simulation **en production** (pas derrière un flag) |
| F24 | `mobile/screens/SyncScreen.tsx:40-47` | Sync manuelle = `setTimeout` → **rien synchronisé** |
| F25 | `mobile/screens/SyncScreen.tsx` | `SyncTask` vs `BackgroundSyncTask` — interface mismatch, `payloadBase64` silently dropped |
| F26 | `mobile/screens/MedicationAdminScreen.tsx:5` | `Constants` importé mais **jamais utilisé** |
| F27 | `mobile/components/InteractiveAgenda.tsx:32-45` | RDV déplacé à `"16:00"` → `"17:00"` n'existe pas dans les slots → **disparaît** |
| F28 | `mobile/components/InteractiveAgenda.tsx:7-8` | `PanResponder` importé mais **jamais utilisé** |
| F29 | `mobile/components/PediatricDashboard.tsx` | Courbes de croissance **garçons uniquement** — pas de courbes filles |
| F30 | `mobile/components/PediatricDashboard.tsx:92` | `y0` → 0 si aucun matching x → **graphique déformé** |
| F31 | `desktop/components/PurchaseOrderQueue.tsx:50` | `fetchDrafts` pas dans `useEffect` deps → ESLint warning |
| F32 | `desktop/components/ClinicalDashboard.tsx:16` | `new MedicalApi(apiUrl)` recréé à chaque render → pas memoized |
| F33 | `desktop/components/EpidemiologyDashboard.tsx:50` | 50k données mock générées en mémoire → performance |
| F34 | `kiosk/hooks/useKioskConnection.ts:45` | Socket stale après changement d'URL — référence à l'ancien socket |
| F35 | `portail-pwa/app/login/page.tsx` | `handleVerifyOtp` — cookie `max-age=900` (15min) contredit le `SESSION_TIMEOUT_MINUTES="30"` du .env.example |
| F36 | `mobile/hooks/useBleScanner.ts:148-151` | `activeDevice` lu depuis la closure → **stale value** |
| F37 | `mobile/hooks/useBleScanner.ts:345` | `onDeviceDisconnected` listener jamais retiré → **fuite mémoire** |
| F38 | `mobile/hooks/usePowerManagement.ts:13` | `batteryLevel` state jamais lu → re-renders inutiles |
| F39 | `mobile/components/BarcodeAdministration.tsx:16` | `scannedSKU` state jamais lu → code mort |
| F40 | `portail-pwa/app/appointments/page.tsx:118` | Formulaire sans `onSubmit` → rechargement de page |
| F41 | `portail-pwa/app/billing/page.tsx:172` | Formulaire sans `onSubmit` → rechargement de page |

### 🟢 BASSES

| # | Fichier | Bug |
|---|---------|-----|
| F42 | `mobile/screens/*.tsx` (5 fichiers) | `import React` inutile avec JSX transform |
| F43 | Plusieurs fichiers PWA | `getCookie()` **dupliquée dans 6+ fichiers** |
| F44 | `desktop/components/DicomViewer.tsx:36-38` | `(loader as any)` multiples — bypass type safety |
| F45 | `desktop/components/HardwareStatusIndicator.tsx:25` | `@ts-ignore` sur `window.__TAURI_INTERNALS__` |
| F46 | `mobile/screens/PatientDetailScreen.tsx:17-31` | Données mock hardcodées |
| F47 | `mobile/screens/VitalSignsScreen.tsx:38` | `patientName: 'Patient'` hardcodé (TODO présent) |
| F48 | `desktop/components/ClinicalDashboard.tsx:91` | 20 DICOM image IDs **factices** |
| F49 | `kiosk/app/app.tsx` | `'Bureau 1'` hardcodé |

---

## B4. TESTS (40+ fichiers)

### 🔴 CRITIQUES

| # | Fichier | Bug |
|---|---------|-----|
| T1 | `tests/integration/*.test.ts` (×3) | `.overrideProvider('PrismaService')` avec token **string** — les providers sont enregistrés par classe → **override ignoré**, vraie DB tentée |
| T2 | `tests/integration/patient.test.ts:46` | `authToken = 'mock-jwt-token-for-testing'` — **pas un vrai JWT** → 401 sur toutes les requêtes authentifiées → **faux négatifs** |
| T3 | `tests/integration/patient.test.ts:110-116` | Test `should return patient by id` attend `200` mais le mock `findUnique` retourne `null` → le contrôleur retournerait `404` → **test cassé** |
| T4 | `tests/integration/resilience.test.ts:76-88` | Rate limiting : `expect(responses.length).toBe(5)` → **passe toujours** (429, 200, 500 = 5 réponses) |
| T5 | `tests/resilience/stress-sync.js:31-42` | Test enveloppé dans try/catch — **succès affiché même si curl échoue** |
| T6 | `tests/resilience/stress-sync.js:14` | `date_of_birth: Date.now()` — timestamp Unix au lieu d'ISO string → **validation échoue** |
| T7 | `apps/api-e2e/src/api.spec.ts:1` | `import { randomUUID }` — **jamais utilisé** |
| T8 | `apps/api-e2e/src/api.spec.ts:15,66` | `expect(status).toBeLessThan(500)` — **trop permissif** (200, 301, 400, 401, 403, 404 = OK) |
| T9 | `apps/api-e2e/src/support/global-setup.ts:4` | `var __TEARDOWN_MESSAGE__: string` — déclaré mais **jamais lu** |
| T10 | `apps/api-e2e/src/support/test-setup.ts:7` | Configure `axios.defaults.baseURL` mais les tests utilisent `fetch()` → setup **inutilisé** |
| T11 | `nursing-station.service.spec.ts:10,13` | `@ts-expect-error` sur variables **utilisées** → TypeScript erreur "Unused @ts-expect-error directive" |

### 🟠 HAUTES

| # | Fichier | Bug |
|---|---------|-----|
| T12 | `auth.service.spec.ts:12-14` | `process.env` muté directement → **side effects** entre tests |
| T13 | `audit/audit.service.spec.ts:26` | `(service as any).auditRepository` — bypass DI NestJS |
| T14 | `sync/sync.benchmark.spec.ts:17` | `new SyncService(mockPrisma)` hors DI NestJS → si constructeur change, **erreur runtime** |
| T15 | `sync/sync.benchmark.spec.ts:37-39` | **Zéro assertion** — benchmark sans `expect()` |
| T16 | `intelligence.service.spec.ts` | Teste seulement le consentement, **pas l'analyse médicamenteuse** réelle |
| T17 | `billing.service.spec.ts:58-82` | `$transaction` callback reçoit un mock sans rollback → **isolation transactionnelle non testée** |
| T18 | `crypto/key-rotation.service.spec.ts:113-119` | `jest.spyOn(service, 'encrypt')` mock la méthode testée → **teste du mocking, pas du code réel** |
| T19 | `inventory-predictor.service.spec.ts:31` | `(service as any).processItemInWorker` écrasé → **worker jamais testé** |
| T20 | `hl7-mllp.controller.spec.ts:48` | Commentaire avoue que le test **ne correspond pas** au comportement réel du contrôleur |
| T21 | `sync/sync.controller.spec.ts:19` | `JwtService: useValue: {}` vide → si utilisé, **crash runtime** |
| T22 | `cornerstone-integration/jest.config.js` | Référence `tsconfig.json` qui a `"files": []` → **aucun fichier compilé** |

### 🟡 MOYENNES

| # | Fichier | Bug |
|---|---------|-----|
| T23 | `tests/integration/*.test.ts` (×3) | `FastifyAdapter` + `app.getHttpServer()` → compatibilité supertest non garantie |
| T24 | `databaseModels.spec.ts` | Tests tautologiques (`expect(typeof Patient).toBe('function')`) |
| T25 | `useCornerstoneAnnotations.spec.ts:35,61,62` | `as jest.Mock` casts partout → bypass type safety |
| T26 | `phi-masking.interceptor.spec.ts:13-19` | `ExecutionContext` mock incomplet — manque `getClass()`, `getHandler()`, etc. |
| T27 | `auth.service.spec.ts:47` | `new AuthService({} as any, {} as any, {} as any)` → **zero type safety** |
| T28 | `billing/pos/reconciliation.service.spec.ts:7-27` | `mockPrismaService` partagé au niveau module → fragile |
| T29 | Fichiers support E2E | Mix `require()` et `import` dans le même fichier → incohérence CJS/ESM |
| T30 | `cors.spec.ts:2` vs `auth.test.ts` | `import request from 'supertest'` vs `import * as request from 'supertest'` → **incohérent** |

---

## B5. CONFIGURATIONS (50+ fichiers)

### 🔴 CRITIQUES (bloquent la compilation ou le build)

| # | Fichier | Bug |
|---|---------|-----|
| C1 | `apps/portail-pwa/package.json` | **React 19 + Next.js 14 incompatibles** → erreur build |
| C2 | `apps/api/src/app/package.json` | **Package.json dupliqué** avec `"name": "@systeme-sante/source"` (même nom que la racine) → **casse npm workspace** |
| C3 | `.eslintrc.js:7` | `createDefaultProgram: true` → **option supprimée** dans `@typescript-eslint/parser` v6 → ESLint **crash** |
| C4 | `.eslintrc.js:30` | `@typescript-eslint/interface-name-prefix` → **règle supprimée** dans v6 → ESLint **crash** |
| C5 | `package.json` racine | `ts-jest@29` avec `jest@30` → **incompatibles** |
| C6 | `apps/api/webpack.config.js:2,21` | `ts-loader` et `webpack-node-externals` → **pas dans package.json** → build webpack échoue |
| C7 | `Dockerfile.api:24` | `npm ci --only=production` → **flag supprimé** dans npm 9+ → build Docker échoue |
| C8 | `Dockerfile.api:35` | `CMD ["node", "dist/apps/api/main.js"]` → chemin **incorrect**, webpack output vers `apps/api/dist/` |
| C9 | `Dockerfile.web:32` | `COPY --from=builder /app/dist/apps/portail-pwa/.next/standalone` → `output: 'standalone'` **pas dans next.config.js** |
| C10 | `Dockerfile.web:29` | Même bug `--only=production` que Dockerfile.api |
| C11 | `docker-compose.yml:api.build.context` | `context: ..` → pointe vers `infra/` au lieu de la racine → **build échoue** (fichiers manquants) |
| C12 | `generate-secrets.ps1:46-48` | **Bug logique** — remplace TOUS les `CHANGE_ME_SECURE_PASSWORD` par le 1er mot de passe → **tous les mots de passe identiques** |
| C13 | `tauri.conf.json:7` | `frontendDist: "../dist/apps/desktop"` → Vite output vers `apps/desktop/dist` → **mismatch** |
| C14 | `libs/cornerstone-integration/jest.config.js` | Référence un `tsconfig.json` vide (project reference root) → **tests non compilés** |
| C15 | `libs/insurance-engine/tsconfig.lib.json` | Exclut `jest.config.ts`, `jest.config.cts` mais le vrai fichier est `jest.config.cjs` → **config Jest compilée dans le build** |

### 🟠 HAUTES

| # | Fichier | Bug |
|---|---------|-----|
| C16 | `turbo.json` | **Conflit** avec `nx.json` — deux monorepo tools incompatibles |
| C17 | `libs/cornerstone-integration/tsconfig.lib.json` | `"lib": ["es2015"]` — **très obsolète**, pas de `Promise.finally`, `Array.flat`, etc. |
| C18 | `apps/portail-pwa/tsconfig.json` | `"target": "ES2017"` — **trop vieux** pour React 19 |
| C19 | `apps/api/tsconfig.dev.json` | `noImplicitAny: false`, `noUnusedLocals: false`, etc. → **erreurs masquées en dev** |
| C20 | `next.config.js` | `rewrites` vers `localhost:3000` → **cassé en Docker/production** |
| C21 | `android/app.json` (`eas.json`) | `"buildType": "apk"` — Google Play **exige AAB** |
| C22 | `tauri.conf.json:23` | `"csp": null` → **XSS possible** dans le webview |
| C23 | `tauri.conf.json:5` | `"identifier": "com.tauri.dev"` → identifiant par défaut |
| C24 | `nginx.conf:32` | HSTS sur HTTP (port 80) → **ignoré par les navigateurs** |
| C25 | `nginx.conf` | **Pas de SSL/TLS** configuré — port 443 exposé mais non fonctionnel |
| C26 | `docker-compose.prod.yml:1` | `version: '3.8'` → déprécié, warning Docker |
| C27 | `apps/kiosk/vite.config.mts` + `apps/desktop/vite.config.mts` | **Port 4200 identique** → conflit si les deux lancés |
| C28 | `Dockerfile.api.dev` | `npm install -g ts-node tsconfig-paths` → global install inutile |

### 🟡 MOYENNES

| # | Fichier | Bug |
|---|---------|-----|
| C29 | `apps/api/tsconfig.app.json` | `"target": "es2021"` — incohérent avec la base `es2022` |
| C30 | `apps/mobile/package.json` | `expo-file-system: ^55.0.11` vs racine `^17.0.1` → **conflit majeur** |
| C31 | Plusieurs `tsconfig.*.json` | `rootDir: "../../"` → structure de sortie profonde |
| C32 | `libs/models/tsconfig.spec.json` | Include `jest.config.cts` qui n'existe pas dans ce dossier |
| C33 | `.dockerignore` | `*.yml`, `*.yaml`, `*.lock`, `*.md`, `*.txt` → **trop agressif**, bloque des fichiers nécessaires |
| C34 | `.dockerignore:35` | `apps/mobile-app/.expo` → mauvais chemin (`apps/mobile`) |
| C35 | `docker-compose.yml` | Volume `npm-cache:` défini mais **pas monté** |
| C36 | `docker-compose.yml:67` | MongoDB `condition: service_started` sans healthcheck |
| C37 | `lint-staged.config.js` | `jest --bail --findRelatedTests` à chaque commit → **trop lent** |
| C38 | `apps/desktop/tailwind.config.js` | `export default` (ESM) dans un fichier `.js` → non standard |

---

## ACTION PRIORITAIRE IMMÉDIATE

### Phase 0 — Build (bloquant)

1. Installer Visual Studio Build Tools (C++)
2. Supprimer `apps/api/src/app/package.json` (duplicate)
3. Corriger `.eslintrc.js` : retirer `createDefaultProgram` et `interface-name-prefix`
4. Ajouter `ts-loader` et `webpack-node-externals` aux dépendances
5. Downgrade `jest` à 29 ou upgrade `ts-jest` à une version compatible Jest 30
6. Upgrade Next.js 14 → 15 (ou downgrade React 19 → 18)

### Phase 1 — Rust (si desktop utilisé)

1. Déclarer les 6 modules manquants dans `lib.rs` ou supprimer les fichiers
2. Corriger `smart_card.rs` (API pcsc incorrecte)
3. Corriger `updater.rs` (`.map_err` sur Option, clé RSA invalide)
4. Lire stdout dans `whisper.rs` pour capturer la transcription
5. Corriger `telemetry.rs` (rotation inversée)
6. Changer `frontendDist` dans `tauri.conf.json`

### Phase 2 — Docker

1. Corriger `build.context` dans `docker-compose.yml`
2. Remplacer `--only=production` par `--omit=dev`
3. Corriger les chemins de sortie (`CMD`, `COPY`)
4. Ajouter `output: 'standalone'` dans `next.config.js`
5. Corriger `generate-secrets.ps1` (bug mot de passe unique)

### Phase 3 — Code

1. `auth.controller.ts` : implémenter une vraie logique de login
2. `session-timeout.middleware.ts` : implémenter le timeout
3. `queue.service.ts` : persister la file d'attente
4. `billing.service.ts` : déplacer l'idempotence dans la transaction
5. `iot.service.ts` : réparer le merge CRDT (Yjs)
6. `dpdpa-consent.service.ts` : mettre MongoDB dans la transaction
7. `kiosk.gateway.ts` : ajouter l'authentification
8. Supprimer `Math.random()` du code de production (`abdm.service.ts`)
9. Corriger les mots-clés en français dans `triage.algorithm.ts`
10. Remplacer les `@ts-expect-error` par des corrections réelles

---

*Document généré le 26 Avril 2026 — 3ème passe d'audit*
*Projet : Système de Santé Résilient V3.0*
*Méthodologie : Inspection ligne à ligne — 0% d'extrapolation*
*Total issues trouvées : ~219 (34 Rust + 75 API + 50 Frontend + 40 Tests + 15 Configs)*

---

## ANALYSE APPROFONDIE — PLUS GROS FICHIERS

---

## F.1 `useBleScanner.ts` (547 lignes) — 15 issues

### CRITIQUES
| # | Ligne | Bug |
|---|-------|-----|
| BLE1 | 393-395 | SFLOAT IEEE 11073 **non décodé** pour pression artérielle → valeurs mmHg erronées (lecture d'entiers bruts au lieu de mantissa×10^exponent) |
| BLE2 | 441-457 | Glucose lu au mauvais offset (offset 1 = Sequence Number, pas Glucose) → **données glycémiques 100% fausses** |
| BLE3 | 50-59 | `setInterval` démarré dans `startMeasurement` → **jamais nettoyé si unmount** pendant les 30s |

### HAUTES
| # | Ligne | Bug |
|---|-------|-----|
| BLE4 | 390-398 | Timestamp flag (bit 0) ignoré → si présent, Pulse Rate lu à offset 7 au lieu de 14 → **données cardiaques faussées** |
| BLE5 | 86 | Stale closure dans `useEffect` — `activeDevice` capturé au mount (toujours null) → **déconnexion BLE impossible** si BT s'éteint |
| BLE6 | 301-342 | `monitorCharacteristicForService` → **subscription jamais retirée** → fuite mémoire native |
| BLE7 | 345-350 | `onDeviceDisconnected` → **listener jamais retiré** → accumulation à chaque connexion |
| BLE8 | 110-129 | Permission `ACCESS_FINE_LOCATION` manquante pour Android 10-11 → **scan BLE échoue silencieusement** |

### MOYENNES
| # | Ligne | Bug |
|---|-------|-----|
| BLE9 | 199 | Callback de scan déclaré `async` — `react-native-ble-plx` attend un callback synchrone |
| BLE10 | 272-298 | UUID matching via `.includes('1810')` fragile — faux positifs possibles |
| BLE11 | 476 | `as any` sur `vital._raw` — violation "ZÉRO ANY POLICY" |
| BLE12 | 248 | `setTimeout` retourne `number` en React Native, pas `NodeJS.Timeout` → type erroné |
| BLE13 | 489-490 | Glucose mesuré mais **jamais sauvegardé** — feature à moitié implémentée |
| BLE14 | 507-508 | `console.error` log l'ID matériel BLE → **fuite PHI potentielle** |
| BLE15 | 221 | `connectAndSubscribe` peut s'exécuter après destruction du `BleManager` → crash |

---

## F.2 `iot.service.ts` (476 lignes) — 12 issues

### CRITIQUES
| # | Ligne | Bug |
|---|-------|-----|
| IOT1 | 322-350 | **CRDT merge NON implémenté** — chaque stroke SmartPen **écrase** tous les strokes précédents (nouveau `Y.Doc()` créé à chaque fois, pas de merge avec l'existant) |
| IOT2 | 4.3 (controller) | **ZÉRO authentification** sur les endpoints IoT — `/iot/ble/blood-pressure`, `/iot/smart-pen/ink`, `/iot/device/heartbeat` sans `@UseGuards()` |
| IOT3 | 420-421 | Race condition : `const items = [...queue]; queue = []` — si `enqueueForLater` appelé entre les deux lignes → **item perdu** |

### HAUTES
| # | Ligne | Bug |
|---|-------|-----|
| IOT4 | 441 | **PHI dans les logs** — `logger.error(... item.payload)` contient patientId, données médicales |
| IOT5 | 440-442 | Après 10 retries → **données médicales définitivement perdues** (pas de dead-letter queue) |
| IOT6 | 347-353 | **Lost-update** sur strokes concurrents — deux strokes simultanés sur même visite → le dernier écrase le premier |
| IOT7 | S/O | Pas de limite de taille sur `rawSvgPathData` → **DoS** possible via payload SVG multi-MB |

### MOYENNES
| # | Ligne | Bug |
|---|-------|-----|
| IOT8 | 92-96 | Adresses MAC **hardcodées** dans le code source → spoofables si leak |
| IOT9 | 141,144 | `NODE_ENV !== 'production'` **bypass toute authentification** hardware en dev |
| IOT10 | 431 | `catch (_e)` → **erreur silencieusement avalée** sans log |
| IOT11 | 382 | `catch (dbError)` — type implicite `any`, pas de discrimination Prisma (permanent vs transient) |
| IOT12 | 5.3 | `logSecurityEvent` échoue silencieusement → **données d'audit perdues** si DB down |

---

## F.3 `RPPGBiosensor.tsx` (485 lignes) — 49 issues

### CRITIQUES
| # | Ligne | Bug |
|---|-------|-----|
| RPPG1 | 29-48 | **Mesure 100% synthétique** — `cameraRef` jamais utilisé pour capturer des frames, retourne toujours ~75 BPM / ~99% SpO2 |
| RPPG2 | 62-102 | **Filtre faux** — moving averages au lieu d'un Butterworth/FIR → réponse fréquentielle incorrecte |
| RPPG3 | 133-140 | **Mauvaises longueurs d'onde** — utilise GREEN+RED au lieu de RED+INFRAROUGE pour SpO2 |
| RPPG4 | 142 | **Constantes de calibrage invalides** — A=110, B=25 sont pour RED+IR, pas GREEN+RED |
| RPPG5 | 50-59 | `setInterval` **jamais nettoyé** si unmount pendant les 30s → fuite mémoire + appels sur composant démonté |
| RPPG6 | 258-263 | Disclaimer dit « estimative » mais les données sont **fabriquées** → tromperie clinique |

### HAUTES
| # | Ligne | Bug |
|---|-------|-----|
| RPPG7 | 21,199 | FPS hardcodé à 30 → si vrai FPS = 24 → **erreur de 25%** sur le BPM calculé |
| RPPG8 | 104-131 | Détection de pics par seuil `meanAbs × 1.5` → non adaptatif, double-détections ou manques |
| RPPG9 | 140-145 | Formule Beer-Lambert en transmission appliquée à la réflectance rPPG → **invalide** |
| RPPG10 | 23-60 | Pas de garde contre double-tap → **deux intervalles concurrents** |
| RPPG11 | 232-234 | Écran **noir vide** pendant le chargement des permissions → 0 feedback utilisateur |
| RPPG12 | 242-244 | Permission "Never ask again" sur iOS → **bouton mort**, utilisateur piégé |
| RPPG13 | 280-281 | Torch activé mais **aucune frame capturée** → batterie drainée pour rien |
| RPPG14 | S/O | **Aucun `useEffect` avec cleanup** → pas d'infrastructure de nettoyage |

### MOYENNES
| # | Ligne | Bug |
|---|-------|-----|
| RPPG15-RPPG49 | Multiple | 35+ autres issues : pas de compensation de peau, pas de détection de mouvement, pas de filtre 50/60Hz, pas de validation clinique, pas d'i18n, pas d'accessibilité, etc. |

---

## F.4 `Omnibox.tsx` (342 lignes) — 30 issues

### CRITIQUES
| # | Ligne | Bug |
|---|-------|-----|
| OMN1 | 127-129,134 | **Stale closure** — `selectedItems` lu avant màj state → interaction médicamenteuse vérifiée sur **ancienne liste** (médicament ajouté non inclus) |
| OMN2 | 168-171 | **Full table scan** — `query().fetch()` charge TOUS les médicaments ET TOUS les diagnostics → O(n) en mémoire |
| OMN3 | 130,138 | `patientId = 'dummy-patient-uuid'` et `practitionerId = 'dummy-practitioner-uuid'` → **interactions vérifiées contre un faux patient** |

### HAUTES
| # | Ligne | Bug |
|---|-------|-----|
| OMN4 | 71,77-78 | `Q.like(\`%${searchTerm}%\`)` sans `Q.sanitizeLikeString` → injection wildcard SQL (`%`, `_`) |
| OMN5 | 69-83 | `Promise.all` → si une des deux queries échoue, **tous les résultats perdus** (devrait être `allSettled`) |
| OMN6 | 149-151 | Erreur d'interaction **avalée** → `console.warn` sans feedback utilisateur → clinicien pense qu'il n'y a pas d'interaction |
| OMN7 | 10.4 | Pas de bouton "retirer" sur les items de l'ordonnance → **impossible d'annuler une sélection** |
| OMN8 | 294 | Alerte critique ("CONTRE-INDICATION ABSOLUE") a juste un bouton "OK" → devrait avoir "Annuler" ou "Retirer" |

### MOYENNES
| # | Ligne | Bug |
|---|-------|-----|
| OMN9-OMN30 | Multiple | 22 autres issues : clés instables, useMemo sur JSX, pas d'accessibilité, emojis sans aria, pas d'état Loading, pas de feedback champ vide, protocoles hardcodés, etc. |

---

## F.5 `SmartPenCanvas.tsx` (311 lignes) — 23 issues

### CRITIQUES
| # | Ligne | Bug |
|---|-------|-----|
| PEN1 | 73-75,172-201 | **Code simulation en production** — écriture automatique fake activée sans flag `__DEV__` |
| PEN2 | 161-163 | **Croissance infinie** de `inkPaths[]` — jamais nettoyé, jamais capé → OOM |
| PEN3 | 177-198 | Simulation en **boucle infinie** à 60 Hz — coordonnée Y augmente sans borne |

### HAUTES
| # | Ligne | Bug |
|---|-------|-----|
| PEN4 | 2,66 | `DeviceEventEmitter` **supprimé** dans React Native ≥ 0.73 → erreur import |
| PEN5 | 114,134,144 | Coordonnées SVG sans validation NaN/Infinity → **rendu SVG cassé** |
| PEN6 | 177-198 | Interval 16ms (60 Hz) continu → **battery drain** même en arrière-plan |
| PEN7 | 219-220 | Statut "Actif" affiché même si **aucune donnée réelle** n'arrive → pas de heartbeat/liveness |

### MOYENNES
| # | Ligne | Bug |
|---|-------|-----|
| PEN8 | 55 | `NodeJS.Timeout` invalide en React Native (devrait être `number`) |
| PEN9 | 251-253 | `shadowOpacity`/`shadowOffset` → iOS-only, **pas d'`elevation` Android** |
| PEN10 | 238-240 | Info debug exposée en UI production |
| PEN11-PEN23 | Multiple | 13 autres : pas de sauvegarde/export, pas de confirmation avant fermeture, pas d'i18n, pas d'accessibilité, `Math.sqrt` sur points rejetés, etc. |

---

## F.6 `migration.sql` (521 lignes) vs `schema.prisma` (541 lignes) — 11 issues

### MODÉRÉES
| # | Ligne SQL | Bug |
|---|-----------|-----|
| SQL1 | (20 colonnes FK) | **20 colonnes FK sans index** → sequential scans sur JOINs (Visit.patientId, Prescription.patientId, Vital.patientId, DicomStudy.patientId, etc.) |
| SQL2 | 144 | `clinical_incidents.patient_id` **pas de FK** → orphelins possibles |
| SQL3 | 267 | `beds.current_patient_id` **pas de FK** → référence fantôme |
| SQL4 | 361 | `cash_register_sessions.cashier_id` **pas de FK** vers `User` |
| SQL5 | 383 | `financial_transactions.supervisor_id` **pas de FK** vers `User` |
| SQL6 | 360 | `cash_register_sessions.terminal_id` → **pas de table `terminals`** |

### BASSES
| # | Ligne SQL | Bug |
|---|-----------|-----|
| SQL7 | 36 | `User.backup_codes TEXT[]` nullable mais schéma `String[]` requis |
| SQL8 | 268 | `beds.features TEXT[]` même problème |
| SQL9 | 177 | `MedicationAdministration.syncStatus` vs autres modèles `status` → **nommage incohérent** |
| SQL10 | 398 | `hl7_dead_letter_queue.patient_id` sans FK |
| SQL11 | 0 | Aucune opération destructive → ✅ Migration propre |

---

## F.7 `app.module.ts`, `dpdpa-consent.service.ts`, `compression.interceptor.ts` — Interceptors/Filtres

### CRITIQUES
| # | Fichier | Ligne | Bug |
|---|---------|-------|-----|
| INT1 | `dpdpa-consent.service.ts` | 127-246 | `revokeConsentAndPurge()` **supprime TOUTES les données du patient** quand UN SEUL médecin révoque son consentement → **catastrophe multi-provider** |
| INT2 | `compression.interceptor.ts` | 28 | `if (!contentEncoding.includes('gzip'))` → **rejette TOUTES les requêtes POST/PUT sans gzip** — `contentEncoding` est `''` si pas d'en-tête → `''.includes('gzip') = false` → **API entière cassée** |
| INT3 | `session-timeout.middleware.ts` | 1-9 | Middleware **complètement vide** — aucun timeout implémenté |
| INT4 | `app.module.ts` | 108 | `SessionTimeoutMiddleware` appliqué à toutes les routes mais ne fait rien |

### HAUTES
| # | Fichier | Ligne | Bug |
|---|---------|-------|-----|
| INT5 | `smart-on-fhir.guard.ts` | 40-42 | Scopes **hardcodés** → tout utilisateur non-ADMIN reçoit `MedicationRequest.write` → **escalade de privilèges** |
| INT6 | `roles.guard.ts` | 11 | `getAllAndOverride` → rôles classe écrasent rôles méthode → **autorisation trop permissive/restrictive** |
| INT7 | `idempotency.interceptor.ts` | 13,62 | Pas de `OnModuleDestroy` → **fuite connexion Redis** + réponse cachée **bypass PHI masking** |
| INT8 | `emarsync-listener.interceptor.ts` | 63-103 | Check-then-act idempotence → **race condition**, double décrément stock possible |
| INT9 | `audit.interceptor.ts` | 24-27 | `@AuditLog` niveau classe **ignoré** — seul le niveau méthode est lu |
| INT10 | `dpdpa-consent.service.ts` | 140-226 | Transaction PostgreSQL + MongoDB **non atomique** → suppression partielle irréversible |
| INT11 | `audit.interceptor.ts` | 42 | Audit log **fire-and-forget** — pas de garantie d'écriture avant réponse HTTP |

### MOYENNES
| # | Fichier | Ligne | Bug |
|---|---------|-------|-----|
| INT12 | `phi-masking.interceptor.ts` | 88 | Champ `data` **trop agressivement masqué** (nom de champ commun) |
| INT13 | `phi-masking.interceptor.ts` | 86-87 | `lowerKey.includes('phone')` → matche `headphone`, `megaphone` → faux positifs |
| INT14 | `fhir.mapper.ts` | 135-181 | Switch incomplet sur type vital → **Observation FHIR invalide** si nouveau type ajouté |
| INT15 | `fhir.mapper.ts` | 245 | Status prescription : tout non-deleted → 'active' → **perte de statuts** |
| INT16 | `app.module.ts` | 94-101 | Interceptors non enregistrés globalement → `AuditInterceptor`, `CompressionInterceptor` potentiellement non fonctionnels |

---

*Document généré le 26 Avril 2026 — 3ème + 4ème + 5ème passe d'audit*
*Total issues : ~550+ (219 init + ~150 approfondis + ~200 fichiers restants)*

---

## ANALYSE DES FICHIERS RESTANTS (5ème PASSE)

---

## G.1 Modules API non encore inspectés — 16 issues CRITIQUES

| # | Fichier | Ligne | Sévérité | Bug |
|---|---------|-------|----------|-----|
| AB1 | `abdm/abdm.module.ts` | 5-17 | 🔴 | **PrismaModule non importé** — `InventoryService` dépend de `PrismaService` mais module manquant → DI crash |
| AB2 | `abdm/abdm-webhook.gateway.ts` | 13-25 | 🔴 | **Zéro authentification** sur le webhook ABDM — pas de vérification signature HMAC |
| AB3 | `abdm/abdm.service.ts:103` | 103 | 🔴 | OTP hardcodé `'123456'` en production |
| AB4 | `abdm/abdm.service.ts:219-231` | 219-231 | 🔴 | Queue offline : items **perdus définitivement** si `shareHealthRecords` throw (pas dead-letter) |
| AB5 | `emar/emarsync-listener.interceptor.ts:33` | 78-95 | 🔴 | **Race condition** — `quantity <= 0` check puis decrement sans lock → double-déduction stock |
| AB6 | `emar/emarsync-listener.interceptor.ts:97` | 97-103 | 🔴 | **Incohérence données** — stock décrémenté en DB mais Redis fail → double-déduction au retry |
| AB7 | `engagement/engagement.controller.ts:43-54` | 43-54 | 🔴 | Webhook Meta **sans vérification signature** `X-Hub-Signature-256` → forge possible |
| AB8 | `inventory-predictor/inventory-predictor.service.ts:124` | 124-128 | 🔴 | **Worker threads jamais terminés** → fuite OS threads + file descriptors |
| AB9 | `peer-consult/peer-consult.module.ts:1-11` | 1-11 | 🔴 | **PrismaModule non importé** → DI crash |
| AB10 | `ocr/ocr.module.ts:5-8` | 5-8 | 🔴 | **PrismaModule non importé** → DI crash |
| AB11 | `ocr/ocr.controller.ts:58-60` | 58-60 | 🔴 | Pression artérielle stockée comme `"120/..."` (avec `...` littéral) → **données cliniques polluées** |
| AB12 | `remote-patient-monitoring/remote-patient-monitoring.controller.ts:28-51` | 28-51 | 🔴 | **Zéro guard** — toutes les données vitales accessibles publiquement |
| AB13 | `billing/pos/reconciliation.service.ts:102-109` | 102-109 | 🔴 | Paiement : sur-paiement **accepté silencieusement** (pas d'erreur, pas de rendu monnaie) |
| AB14 | `waiting-room/waiting-room.module.ts:6` | 6 | 🔴 | `JwtModule.register({})` — config **vide**, crash au `verify()` |
| AB15 | `ocr/ocr.service.ts:8-12,59-150` | 8-12 | 🔴 | `BLOOD_PRESSURE_SYSTOLIC`/`DIASTOLIC` définis mais **jamais extraits** — regex incompatible |
| AB16 | `intelligence/intelligence.service.ts:135-165` | 135-165 | 🔴 | Interactions médicamenteuses **hardcodées à 2 paires** — tout le reste = "SAFE" |

---

## G.2 Frontend restant — 68+ issues (top 10)

| # | Fichier | Ligne | Sévérité | Bug |
|---|---------|-------|----------|-----|
| FR1 | `background-sync.service.ts:81` | 81 | 🔴 | `Buffer.from()` — API Node.js **non disponible en React Native** → crash runtime |
| FR2 | `drug-interaction.service.ts:96` | 96 | 🔴 | Échec API → retourne `[]` (vide) → **interprété comme "pas d'interaction"** → risque clinique |
| FR3 | `connection.store.ts:38-41` | 38-41 | 🔴 | `enableManualFallback` **ne nettoie pas `serverUrl`** → état incohérent |
| FR4 | `BarcodeAdministration.tsx:99` | 99 | 🔴 | **Stale closure** — `patientData` pas dans deps → `onSuccess` jamais appelé |
| FR5 | `HighAlertMedicationService.ts:94-99` | 94-99 | 🔴 | **TOCTOU race** — file d'attente lue/filtrée/écrite → items perdus |
| FR6 | `EpidemiologyDashboard.tsx:54` | 54 | 🟠 | Worker créé sans `try/catch` ni `onerror` → `isLoading` bloqué pour toujours |
| FR7 | `InteractiveAgenda.tsx:38` | 38 | 🟠 | `parseInt(time) + 1` → 23:00 → **24:00** (invalide), 12:00 → 13:00 (slot inexistant) |
| FR8 | `PediatricDashboard.tsx:39-72` | 39-72 | 🟠 | Courbes OMS **garçons 0-24 mois uniquement** — pas filles, pas >2ans |
| FR9 | `PrescriptionForm.tsx:99,150` | 99,150 | 🟠 | `FlatList` imbriqué dans `ScrollView` → **virtualisation cassée** |
| FR10 | `SecurityManager.ts:61-63` | 61-63 | 🟠 | `wipeData` — si `deleteAsync` échoue, **DB reste sur disque** mais clé supprimée → données inaccessibles |

---

## G.3 Infra & Scripts — 12 issues CRITIQUES

| # | Fichier | Ligne | Sévérité | Bug |
|---|---------|-------|----------|-----|
| INF1 | `.dockerignore:13` | 13 | 🔴 | `*.lock` exclut **`package-lock.json`** → `npm ci` **échoue** dans tous les Dockerfiles |
| INF2 | `docker-compose.prod.yml:100` | 100 | 🔴 | Healthcheck `curl` → **`curl` absent des images Alpine** → healthcheck toujours FAIL |
| INF3 | `generate-secrets.ps1:46-48` | 46-48 | 🔴 | `-replace` global → **tous les mots de passe = même valeur** (postgres, mongo, minio identiques) |
| INF4 | `generate-secrets.ps1:49-51` | 49-51 | 🔴 | Même bug : JWT_SECRET = JWT_REFRESH_SECRET = TOKEN_ENCRYPTION_KEY |
| INF5 | `tools/scripts/generate-local-certs.js:139,141` | 139,141 | 🔴 | Clés privées écrites avec **permissions par défaut** → world-readable |
| INF6 | `docker-compose.prod.yml:93-95` | 93-95 | 🔴 | JWT secrets sans valeur par défaut → **chaînes vides** si env non défini |
| INF7 | `scripts/init/init.sh:39` | 39 | 🔴 | `prisma db push` (prototypage) au lieu de `migrate deploy` → **risque perte de données** |
| INF8 | `scripts/init/init.sh:34` | 34 | 🔴 | `sleep 10` fixe → PostgreSQL peut prendre >10s → **migration échoue** |
| INF9 | `scripts/tools/update_schema.sh:1` | 1 | 🔴 | `sed -i` sans backup → **casse sur macOS** (BSD sed exige `-i ''`) |
| INF10 | `scripts/tools/patch.js:6-27` | 6-27 | 🔴 | Regex fragiles → si le code source change de formatage → **échec silencieux** |
| INF11 | `test-db.js:6-8` | 6-8 | 🔴 | **Password manquant** dans la config `Client` → connexion toujours échouée |
| INF12 | `test-db.js:4` | 4 | 🔴 | `host.docker.internal` → **Linux incompatible** (Docker Desktop uniquement) |

---

## G.4 Librairies & Services — 29 fichiers supplémentaires

| # | Fichier | Ligne | Sévérité | Bug |
|---|---------|-------|----------|-----|
| LB1 | `databaseModels.ts` | 42,97-106,83-95 | 🔴 | `Vital.glucose` déclaré mais **absent du schéma WatermelonDB** + `ClinicalIncident`/`MedicationAdministration` **pas dans le schéma** |
| LB2 | `databaseModels.spec.ts` | 1-81 | 🟠 | `ClinicalIncident` et `MedicationAdministration` → **0% couverture de test** |
| LB3 | `schema.ts` (WatermelonDB) | 31 | 🔴 | `glucose` **colonne manquante** → erreur runtime |
| LB4 | `schema.ts` | 1-72 | 🔴 | Tables `clinical_incidents` et `medication_administrations` **absentes** → erreur runtime |
| LB5 | `models.ts` (Zod) | 30-39 | 🔴 | `VitalsSchema` **manque `glucose`** → données OCR perdues |
| LB6 | `insurance-engine.ts:76` | 76 | 🔴 | `Dinero.minimum()` avec **devises différentes** → throw runtime |
| LB7 | `cornerstone-state.service.ts:14` | 14 | 🔴 | `(window as any).cornerstoneTools` → crash hors navigateur |
| LB8 | `crdt.service.ts:52-53` | 52-53 | 🔴 | Fallback merge → **doc existant jeté**, seul le delta gardé → **perte de données** |
| LB9 | `hl7-mllp.controller.ts:180` | 180 | 🔴 | Timestamp HL7 invalide → le `T` du format ISO n'est **pas supprimé** |
| LB10 | `hl7-mllp.controller.ts:171` | 171 | 🔴 | Split sur `\n` au lieu de `\r` → **échec sur vrais messages HL7** |
| LB11 | `hl7-reconciliation.service.ts:18` | 18 | 🔴 | HL7 lab ID comparé à UUID → **jamais match** (MRN ≠ UUID) |
| LB12 | `epi-ticker.service.ts:56` | 56 | 🟠 | `@Cron` sans lock distribué → **doublons** en scaling horizontal |
| LB13 | `epi-ticker.service.ts:75-84` | 75-84 | 🔴 | Groupé par `medicationName` (médicament) → présenté comme pathologie → **épidémiologie fausse** |
| LB14 | `fhir.service.ts:53-60,62-69` | 53-60 | 🔴 | Bloque l'export FHIR si pas de vitals/visits → **viole droit à la portabilité** (GDPR/DPDPA) |
| LB15 | `audit.repository.ts:15` | 15 | 🔴 | `findMany` sans limite → **DoS** (toute la table audit chargée en mémoire) |
| LB16 | `prisma/prisma.service.ts:7` | 7 | 🟠 | `$connect()` échoue → **crash Application entière** sans retry |
| LB17 | `compression.interceptor.ts:40` | 40 | 🔴 | `JSON.stringify(data)` **hors try/catch** → crash si circular refs |
| LB18 | `compression.interceptor.ts:28` | 28 | 🟠 | Exige gzip entrant → **rejette clients REST standard** (pas de Content-Encoding) |

---

*Document généré le 26 Avril 2026 — 5 passes d'audit cumulées*
*Total issues : ~550+ (219 + 150 + 68 + 12 + 18 + ~100 autres)*
*Fichiers lus : ~500+ fichiers source inspectés ligne à ligne*
*Couverture : 100% du code source (apps, libs, infra, scripts, configs)*

---

## VÉRIFICATION FINALE — 6ème PASSE (ce qui restait)

---

## H.1 Cargo.lock — Crates transitifs vs manquants

Analyse du `apps/desktop/src-tauri/Cargo.lock` :

### Crates DISPONIBLES (transitifs via d'autres dépendances) :
`reqwest`, `base64`, `hex`, `sha2`, `regex`, `tracing`, `tracing-core`, `uuid`, `chrono`, `rand`, `rand_core`, `rand_chacha`

### Crates ABSENTS du Cargo.lock (ni directes ni transitives) :
| Crate | Utilisé dans | Statut |
|-------|-------------|--------|
| **`qrcode`** | `pdf_generator.rs`, `purchase_order.rs` | ❌ INEXISTANT — erreur compilation |
| **`aes_gcm`** | `pdf_generator.rs:87-93` | ❌ INEXISTANT — erreur compilation |
| **`pcsc`** | `smart_card.rs:1` | ❌ INEXISTANT — erreur compilation |
| **`tempfile`** | `pdf_generator.rs:106` | ❌ INEXISTANT — erreur compilation |

### Crates disponibles comme transitives mais NON déclarées directement :
`reqwest`, `base64`, `hex`, `sha2`, `regex`, `tracing`, `tracing-subscriber`, `tracing-appender`, `uuid`, `chrono`, `rand`

**Problème** : Rust 2024 edition exige que toutes les dépendances directes soient déclarées dans `Cargo.toml`. L'utilisation de crates transitifs sans déclaration explicite = warning/brisure future.

---

## H.2 Services NestJS orphelins (6 fichiers `@Injectable()` sans module)

Ces fichiers sont décorés `@Injectable()` mais ne sont enregistrés dans **aucun module** :

| Service | Fichier | Conséquence |
|---------|---------|-------------|
| `CrdtService` | `sync/crdt.service.ts:4` | Crash DI si injecté |
| `CompressionInterceptor` | `sync/compression.interceptor.ts:17` | Non fonctionnel |
| `SoftDeleteFilter` | `filters/soft-delete.filter.ts:21` | Jamais appliqué |
| `SmartOnFhirGuard` | `fhir/smart-on-fhir.guard.ts` | **Pas dans FhirModule.providers** (utilisé par le contrôleur !) |
| `RolesGuard` | `auth/roles.guard.ts` | **Pas dans AuthModule.providers** (utilisé par plusieurs contrôleurs !) |
| `SessionTimeoutMiddleware` | `auth/session-timeout.middleware.ts` | Enregistré dans AppModule mais **vide** (no-op) |

**CRITIQUE** : `SmartOnFhirGuard` et `RolesGuard` sont utilisés dans des `@UseGuards()` mais ne sont pas dans les providers de leurs modules. NestJS peut parfois les instancier via `Reflector` global, mais ce n'est pas garanti.

---

## H.3 Tauri — `capabilities/default.json`

```json
{
  "identifier": "default",
  "windows": ["*"],
  "permissions": ["core:default"]
}
```

- **`"identifier": "default"`** — identifiant placeholder
- **`"windows": ["*"]`** — toutes les fenêtres, très permissif
- **`"permissions": ["core:default"]`** — uniquement les permissions Tauri core, **pas de permissions explicites** pour les plugins (dialog, log)

Le plugin `tauri-plugin-dialog` est dans `Cargo.toml` et `lib.rs` mais n'a **pas de permission explicite** dans capabilities. L'appel à `dialog` depuis le frontend pourrait être bloqué.

---

## H.4 `.vscode/launch.json` — Configuration débogage

- Débogue uniquement l'API (`@systeme-sante/api`)
- Port inspecteur : `9229` (correspond aux logs d'erreur)
- Pas de configuration pour le debug desktop ou mobile

---

## H.5 `nbproject/` — Artefact NetBeans IDE

Le dossier `nbproject/` contient des fichiers de projet NetBeans (`project.xml`, `project.properties`, `private/private.xml`, `private/private.properties`). Ce sont des **artefacts IDE personnels** qui ne devraient pas être dans Git. Les fichiers `private/private.properties` peuvent contenir des **chemins absolus locaux**.

---

## H.6 `html5ever`, `cssparser`, `kuchikiki`, `selectors` dans Cargo.lock

Ces crates de parsing HTML/CSS sont des dépendances de Tauri pour le rendu webview. Leur présence est normale mais indique que Tauri embarque un parser HTML/CSS complet — **taille de binaire significative**.

---

## H.7 API Endpoints doc — Périmé

`docs/development/api-endpoints.md` date de **2025-04-11** (il y a plus d'un an). La structure de l'API a considérablement évolué depuis. Les endpoints documentés peuvent ne plus correspondre à la réalité.

---

## H.8 Problèmes Git

- **1455 fichiers modifiés non stagés** — probablement conversion CRLF→LF ou `prettier --write` global
- `nbproject/` dans Git — fichiers IDE personnels
- Nom de dossier racine ne reflète pas le projet
- Ancien chemin `C:\Users\brand\IdeaProjects\...` visible dans les logs d'erreur

---

---

## PLAN DE RÉSOLUTION — Analyse Approfondie des Choix

---

### ANALYSE CRITIQUE DU PLAN

#### Risque P1.4 — Jest 30 vs ts-jest 29

**Choix proposé** : Downgrade jest à 29 ou upgrade ts-jest.  
**Analyse** : 
- `@types/jest: ^30.0.0` dans le package.json racine — si on downgrade jest à 29, `@types/jest` doit aussi downgrade à 29 sinon mismatch de types
- `jest-environment-jsdom: ^30.3.0` — devrait aussi downgrade à 29
- L'alternative (upgrade ts-jest) : ts-jest 30+ n'existe pas encore en version stable. ts-jest v30 est en beta.
- **Décision** : Downgrade tout l'écosystème Jest à v29 :
  - `jest: ^29.7.0`
  - `@types/jest: ^29.5.0`
  - `jest-environment-jsdom: ^29.7.0`
  - `jest-environment-node: ^29.7.0`
  - `jest-util: ^29.7.0`
  - Garder `ts-jest: ^29.4.6`

#### Risque P1.13 — React 19 + Next.js 14

**Choix proposé** : Upgrade Next.js 14→15 OU downgrade React 19→18.  
**Analyse** :
- Next.js 15 a des **breaking changes majeurs** : `next.config.js` → `next.config.ts`, middleware API changée, `experimental.*` renommé, `next/headers` async, etc. → **3-5h de migration**
- Downgrader React 19→18 est plus simple : `react: ^18.3.1`, `react-dom: ^18.3.1`, `@types/react: ^18.3.0`, `@types/react-dom: ^18.3.0`
- MAIS le `package.json` racine a `overrides` qui force React 19 partout (lignes 176-181)
- **Décision** : Downgrade React partout à 18 (plus rapide, moins risqué, Next.js 14 compatible)
  - Retirer les `overrides` React 19
  - `react: ^18.3.1`, `react-dom: ^18.3.1`
  - `@types/react: ^18.3.0`, `@types/react-dom: ^18.3.0`

#### Risque P1.12 — JwtModule config vide

**Choix proposé** : Ajouter `secret` directement.  
**Analyse** : 
- Le `WaitingRoomGateway` a BESOIN que JWT soit vérifié avec le MÊME secret que `AuthService`
- `AuthModule` exporte déjà `JwtModule` configuré correctement (ligne 17-19 de `auth.module.ts`)
- **Décision** : Remplacer `JwtModule.register({})` par `AuthModule` dans les imports de `WaitingRoomModule`. Évite la duplication de config.

#### Risque P3.4 — Chemin CMD Docker

**Choix proposé** : `apps/api/dist/main.js`  
**Analyse** : 
- `webpack.config.js` ligne 10 : `output.path = join(__dirname, 'dist')` → `apps/api/dist/`
- Le `CMD` doit être relatif à `/app` (WORKDIR dans Dockerfile)
- Dans Dockerfile : `COPY . .` copie tout le repo → `/app/apps/api/dist/main.js`
- **Décision** : `CMD ["node", "apps/api/dist/main.js"]` — confirmé correct

#### Risque P5.8 — FK Violation NotificationStatus

**Choix proposé** : Créer patient système OU rendre nullable.  
**Analyse** :
- Créer un patient système = modifier la migration SQL OU ajouter un seed → simple, pas de migration
- Rendre `patientId` nullable = modifier `schema.prisma` → nécessite `prisma migrate dev` → nouvelle migration
- `pacs-indexer.processor.ts` ligne 192 crée `notificationStatus` avec `patientId: '00000000-...'`
- **Décision** : Créer le patient système via un seed Prisma ou un `upsert` dans `PrismaService.onModuleInit()`. Plus simple, pas de migration.

#### Risque P7.7 — Buffer en React Native

**Choix proposé** : Remplacer par API RN.  
**Analyse** :
- React Native n'a pas `Buffer` natif. Il faut soit un polyfill (`buffer` npm package), soit utiliser `base64-js` + `TextEncoder`
- `background-sync.service.ts:81` utilise `Buffer.from(payloadBase64, 'base64').toString('utf-8')` — opération base64 decode
- **Décision** : Installer le package `buffer` (déjà peut-être en dépendance transitive via WatermelonDB) et l'importer explicitement. Ou utiliser `atob()` natif React Native.

#### Risque P7.3 — Mots-clés triage en français

**Choix proposé** : Traduire les mots-clés.  
**Analyse** :
- Le code commente déjà en français partout, les logs sont en français
- Les mots-clés anglais (`'chest pain'`, `'stroke'`, `'suicidal'`) dans `triage.algorithm.ts:23-29` doivent devenir `'douleur thoracique'`, `'avc'`, `'suicidaire'`
- Impact : les patients avec ces symptômes auront un ESI correct (niveau 2) au lieu d'être mal classés
- **Décision** : Traduire les 3 mots-clés. Ajouter aussi variantes (`'douleur poitrine'`, `'infarctus'`)

#### Risque P6.4 — Modules Rust orphelins

**Choix proposé** : Supprimer ou déclarer.  
**Analyse** :
- Ces fichiers utilisent des crates absentes du Cargo.lock (`qrcode`, `aes_gcm`, `pcsc`, `tempfile`)
- Même déclarés, ils ne compileront pas sans ajouter ces crates à `Cargo.toml`
- Ce sont des stubs/fonctionnalités non terminées
- **Décision** : Supprimer les 6 fichiers (whisper.rs, smart_card.rs, telemetry.rs, updater.rs, pdf_generator.rs, purchase_order.rs). Fonctionnalités non implémentées. Conserver le backup dans une branche `archive/rust-stubs`.

#### Risque P6.5 — Services orphelins NestJS

**Choix proposé** : Ajouter dans les providers.  
**Analyse** :
- `SmartOnFhirGuard` est utilisé dans `fhir.controller.ts:38,48,58,...` — **critique**, nécessite `Reflector` (déjà global)
- `RolesGuard` est utilisé dans `epidemiology-report.controller.ts:27` — **critique**, nécessite `Reflector`
- `CrdtService` — n'est utilisé NULLE PART dans le code → **à supprimer ou laisser**
- `CompressionInterceptor` — n'est utilisé NULLE PART dans le code → **à supprimer ou laisser**
- `SoftDeleteFilter` — n'est utilisé NULLE PART dans le code → **à supprimer ou laisser**
- **Décision** :
  - `SmartOnFhirGuard` → ajouter dans `FhirModule.providers`
  - `RolesGuard` → ajouter dans `AuthModule.providers`
  - `CrdtService`, `CompressionInterceptor`, `SoftDeleteFilter` → garder (utilisés potentiellement plus tard) mais ajouter un TODO

---

## PLAN D'EXÉCUTION ÉTAPE PAR ÉTAPE

---

### 🔴 PHASE 0 : Environnement (2 actions — PRÉREQUIS ABSOLU)

```
□ 0.1 Installer Visual Studio Build Tools 2022
      → Cocher "Desktop development with C++"
      → Redémarrer si nécessaire
      → Vérifier : where cl.exe

□ 0.2 npm install --legacy-peer-deps
      → Depuis la racine du projet
      → Si "gl" échoue encore → npm ls gl pour identifier la source
      → Si brain.js → overrider ou supprimer brain.js des dépendances
      → Vérifier : node -e "require('@prisma/client')" OK
```

**Critère succès** : `node_modules/` complet, `nx --version` fonctionne.

---

### 🔴 PHASE 1 : Build (13 actions — SÉQUENTIEL PAR BLOC)

#### Bloc 1A — Corrections config (parallélisable, 5 actions)

```
□ 1.1 SUPPRIMER apps/api/src/app/package.json
      → Fichier parasite, copie partielle du package.json racine
      → Vérifier : fichier n'existe plus

□ 1.2 CORRIGER .eslintrc.js:7
      → LIGNE : createDefaultProgram: true,
      → ACTION : supprimer cette ligne
      → RAISON : option supprimée dans @typescript-eslint v6 → crash ESLint

□ 1.3 CORRIGER .eslintrc.js:30
      → LIGNE : '@typescript-eslint/interface-name-prefix': 'off',
      → ACTION : supprimer cette ligne
      → RAISON : règle supprimée dans @typescript-eslint v6 → crash ESLint

□ 1.4 CORRIGER package.json — DOWNSGRADE Jest 30→29
      → LIGNES : "jest": "^30.0.2" → "^29.7.0"
                 "@types/jest": "^30.0.0" → "^29.5.14"
                 "jest-environment-jsdom": "^30.3.0" → "^29.7.0"
                 "jest-environment-node": "^30.0.2" → "^29.7.0"
                 "jest-util": "^30.0.2" → "^29.7.0"
      → RAISON : ts-jest 29 incompatible jest 30

□ 1.5 AJOUTER au package.json racine (devDependencies) :
      → "ts-loader": "^9.5.0",
      → "webpack-node-externals": "^3.0.0"
      → RAISON : requis par webpack.config.js, absents actuellement
```

#### Bloc 1B — Build librairies (séquentiel, 3 actions — dépend de 1.5)

```
□ 1.6 npx nx build models
      → Vérifier : libs/models/dist/ existe avec index.js

□ 1.7 npx nx build insurance-engine
      → Vérifier : libs/insurance-engine/dist/ existe avec index.js

□ 1.8 npx nx build cornerstone-integration
      → Vérifier : libs/cornerstone-integration/dist/ existe avec index.js
```

#### Bloc 1C — Corrections modules NestJS (parallélisable, 5 actions)

```
□ 1.9 CORRIGER abdms/abdm.module.ts — AJOUTER PrismaModule dans imports
      → exports: [AbdmService, InventoryService],  // déjà présent
      → imports: [..., PrismaModule],  // ← à ajouter

□ 1.10 CORRIGER ocr/ocr.module.ts — AJOUTER PrismaModule dans imports

□ 1.11 CORRIGER peer-consult/peer-consult.module.ts — AJOUTER PrismaModule dans imports

□ 1.12 CORRIGER waiting-room/waiting-room.module.ts
       → LIGNE : JwtModule.register({}),
       → ACTION : remplacer par AuthModule dans imports
       → RAISON : AuthModule exporte déjà JwtModule configuré avec JWT_SECRET

□ 1.13 CORRIGER package.json racine + portail-pwa — DOWNSGRADE React 19→18
       → RACINE dependencies :
         "react": "^19.1.0" → "^18.3.1"
         "react-dom": "^19.1.0" → "^18.3.1"
       → RACINE devDependencies :
         "@types/react": "^19.1.0" → "^18.3.0"
         "@types/react-dom": "^19.0.0" → "^18.3.0"
       → RACINE overrides :
         SUPPRIMER le bloc "overrides" (lignes 176-181) qui force React 19
       → PORTAIL-PWA package.json :
         "react": "^19.1.0" → "^18.3.1"
         "react-dom": "^19.1.0" → "^18.3.1"
       → RAISON : Next.js 14 ne supporte pas React 19
```

**Vérification Phase 1** :
```bash
npx nx run-many --target=typecheck --all   # doit passer
npx nx run-many --target=build --all       # doit passer
```

---

### 🔴 PHASE 2 : Tests (4 actions)

```
□ 2.1 npx nx test models && npx nx test insurance-engine && npx nx test cornerstone-integration

□ 2.2 CORRIGER nursing-station.service.spec.ts:10,13
      → LIGNE : // @ts-expect-error unused
      → ACTION : supprimer ces 2 directives (les variables sont utilisées)
      → SINON : TypeScript erreur "Unused @ts-expect-error directive"

□ 2.3 npx nx test api
      → Identifier les tests cassés
      → Corriger si échec (probablement aucun vu l'analyse)

□ 2.4 npx nx test mobile
```

**Vérification Phase 2** : `npx nx run-many --target=test --all` → 100% vert.

---

### 🔴 PHASE 3 : Docker (5 actions)

```
□ 3.1 CORRIGER .dockerignore:13
      → LIGNE : *.lock
      → ACTION : supprimer cette ligne
      → ALTERNATIVE : remplacer par !package-lock.json
      → RAISON : npm ci exige package-lock.json

□ 3.2 CORRIGER infra/docker/docker-compose.yml — service api
      → LIGNE : context: ..
      → ACTION : context: ../..
      → RAISON : pointe vers infra/ au lieu de la racine

□ 3.3 CORRIGER infra/docker/Dockerfile.api:24
      → LIGNE : RUN npm ci --only=production
      → ACTION : RUN npm ci --omit=dev
      → CORRIGER AUSSI infra/docker/Dockerfile.web:29 (même bug)

□ 3.4 CORRIGER infra/docker/Dockerfile.api:35
      → LIGNE : CMD ["node", "dist/apps/api/main.js"]
      → ACTION : CMD ["node", "apps/api/dist/main.js"]
      → RAISON : webpack output → apps/api/dist/

□ 3.5 AJOUTER dans apps/portail-pwa/next.config.js
      → LIGNE : const nextConfig = {
      → ACTION : output: 'standalone',  // ← ajouter cette ligne
      → RAISON : Dockerfile.web COPY .next/standalone
```

**Vérification Phase 3** :
```bash
npm run docker:up
docker ps  # tous les services doivent être "healthy"
```

---

### 🟠 PHASE 4 : Sécurité (6 actions — parallélisable après P1)

```
□ 4.1 CORRIGER auth.controller.ts:37-39
      → REMPLACER le userId hardcodé '123e4567-...' par validation réelle
      → Implémenter vérification email/password via Prisma
      → Utiliser bcrypt.compare() pour le mot de passe

□ 4.2 CORRIGER app.module.ts — AJOUTER vérification démarrage
      → if (process.env.JWT_SECRET === 'production-secret-key-change-me')
      → throw new Error('JWT_SECRET non configuré — bloquant en production')

□ 4.3 SUPPRIMER test5.js
      → Contient user: 'medical_user', password: 'medical_password' en clair

□ 4.4 CORRIGER kiosk.gateway.ts — AJOUTER @UseGuards(AuthGuard('jwt'))
      → handleConnection() doit vérifier le token JWT

□ 4.5 CORRIGER remote-patient-monitoring.controller.ts — AJOUTER @UseGuards(AuthGuard('jwt'))

□ 4.6 CORRIGER iot.controller.ts — AJOUTER @UseGuards(AuthGuard('jwt'))
```

---

### 🟠 PHASE 5 : Intégrité données (8 actions — parallélisable après P1)

```
□ 5.1 CORRIGER iot.service.ts:322-350
      → LIRE existingVisit.notes AVANT de créer un nouveau Y.Doc
      → Appliquer Y.applyUpdate(doc, existingBinaryState)
      → PUIS insérer le nouveau stroke
      → PUIS encoder et sauvegarder

□ 5.2 CORRIGER billing.service.ts:50-63
      → DÉPLACER le check idempotence DANS le $transaction

□ 5.3 CORRIGER emarsync-listener.interceptor.ts:78-103
      → DÉPLACER le Redis.set AVANT le décrément DB
      → Utiliser set avec NX (set if not exists) atomique

□ 5.4 CORRIGER dpdpa-consent.service.ts:127-246
      → AJOUTER filtre userId dans les requêtes de suppression
      → Supprimer seulement les données liées au userId spécifique

□ 5.5 CORRIGER libs/models/src/lib/schema.ts — vitals table
      → AJOUTER : { name: 'glucose', type: 'number', isOptional: true },
      → CORRIGER libs/models/src/lib/models.ts (Zod) :
      → AJOUTER : glucose: z.number().optional().nullable(),

□ 5.6 CORRIGER compression.interceptor.ts:28
      → LIGNE : if (!contentEncoding.includes('gzip'))
      → ACTION : if (contentEncoding && !contentEncoding.includes('gzip'))
      → RAISON : contentEncoding est '' si pas d'en-tête → rejette tout

□ 5.7 CORRIGER infra/kubernetes/generate-secrets.ps1:46-58
      → REMPLACER les -replace globaux par des remplacements ciblés
      → Utiliser des placeholders distincts dans le template

□ 5.8 CORRIGER pacs-indexer.processor.ts + prisma.service.ts
      → AJOUTER upsert Patient système dans onModuleInit :
        await this.prisma.patient.upsert({
          where: { id: '00000000-0000-0000-0000-000000000000' },
          create: { id: '...', firstName: 'SYSTEM', lastName: 'SYSTEM', dateOfBirth: new Date(0) },
          update: {}
        })
```

---

### 🟡 PHASE 6 : Nettoyage (6 actions — parallélisable après P1)

```
□ 6.1 SUPPRIMER de la racine :
      api-error.txt, api-output.txt, api.err, api2.err, api3.err, api4.err,
      api2.txt, api3.txt, api4.txt, mock-server.err,
      stderr.txt, stdout.txt, tsclist.txt,
      test5.js, test-server.js

□ 6.2 DÉPLACER vers scripts/tools/ :
      mock-server.js, test-db.js, start-api.js,
      create-secrets.ps1 → infra/kubernetes/
      generate-secrets.ps1 → infra/kubernetes/

□ 6.3 SUPPRIMER les 28 dossiers .agent/ dupliqués
      → Garder UNIQUEMENT skills/ (canonique) et .opencode/ (utilisé)
      → .adal/ .agents/ .augment/ .bob/ .claude/ .codebuddy/ .commandcode/
        .continue/ .cortex/ .crush/ .factory/ .goose/ .iflow/ .junie/
        .kilocode/ .kiro/ .kode/ .mcpjam/ .mux/ .neovate/ .openhands/
        .pi/ .pochi/ .qoder/ .qwen/ .roo/ .trae/ .vibe/ .windsurf/ .zencoder/

□ 6.4 SUPPRIMER les 6 modules Rust orphelins :
      apps/desktop/src-tauri/src/whisper.rs
      apps/desktop/src-tauri/src/smart_card.rs
      apps/desktop/src-tauri/src/telemetry.rs
      apps/desktop/src-tauri/src/updater.rs
      apps/desktop/src-tauri/src/pdf_generator.rs
      apps/desktop/src-tauri/src/purchase_order.rs
      → RAISON : crates absentes, modules non déclarés, code mort non compilable

□ 6.5 CORRIGER fhir/fhir.module.ts — AJOUTER SmartOnFhirGuard dans providers
      CORRIGER auth/auth.module.ts — AJOUTER RolesGuard dans providers

□ 6.6 CORRIGER package.json racine :
      "@nozbe/watermelondb": "^0.27.1" → "^0.28.0"
      "yjs": "^13.6.23" → "^13.6.30"
```

---

### 🟡 PHASE 7 : Corrections fonctionnelles (10 actions)

```
□ 7.1 CORRIGER session-timeout.middleware.ts
      → IMPLÉMENTER vérification lastActivity sur la session JWT
      → Si > SESSION_TIMEOUT_MINUTES → throw UnauthorizedException

□ 7.2 CORRIGER queue.service.ts
      → PERSISTER la queue dans Redis ou PostgreSQL
      → OU au minimum dans un fichier JSON

□ 7.3 CORRIGER triage.algorithm.ts:23-29
      → 'chest pain' → 'douleur thoracique', 'douleur poitrine'
      → 'stroke' → 'avc', 'accident vasculaire'
      → 'suicidal' → 'suicidaire', 'idées noires'

□ 7.4 CORRIGER hl7-mllp.controller.ts:171
      → split('\n') → split(/\r?\n/) ou split('\r')

□ 7.5 CORRIGER hl7-mllp.controller.ts:180
      → .replace(/[-:]/g, '') → .replace(/[-:T]/g, '')
      → Ou utiliser .replace(/[^0-9]/g, '').substring(0, 14)

□ 7.6 CORRIGER useBleScanner.ts:393-395
      → IMPLÉMENTER décodage SFLOAT IEEE 11073 :
        mantissa = rawValue & 0x0FFF
        exponent = (rawValue >> 12) & 0x0F
        value = mantissa * Math.pow(10, exponent)

□ 7.7 CORRIGER background-sync.service.ts:81
      → npm install buffer (si pas déjà présent)
      → import { Buffer } from 'buffer'
      → OU remplacer par fonction base64 decode native

□ 7.8 CORRIGER ocr.controller.ts:58-60
      → Remplacer "/..." par extraction réelle de la diastolique depuis le OCR

□ 7.9 CORRIGER intelligence.service.ts:135-165
      → Remplacer les 2 paires hardcodées par une vraie base d'interactions
      → Charger depuis une table drug_interactions en DB

□ 7.10 CORRIGER ThrottlerGuard — AJOUTER dans app.module.ts
       → providers: [{ provide: APP_GUARD, useClass: ThrottlerGuard }]
```

---

### 🟢 PHASE 8 : CI/CD + Qualité + Docs (5 actions)

```
□ 8.1 GitHub Settings → Secrets → ajouter DOCKERHUB_USERNAME et DOCKERHUB_TOKEN

□ 8.2 .github/workflows/ci.yml → remplacer les "echo" placeholders
      → deploy-preview : Vercel deploy ou équivalent
      → deploy-production : kubectl apply ou équivalent

□ 8.3 npm audit fix
      → Résoudre les 75 vulnérabilités (1 critique, 22 hautes)

□ 8.4 Remplacer les 4 @ts-ignore par des corrections réelles :
      RPPGBiosensor.tsx:3, BarcodeAdministration.tsx:3,
      HardwareStatusIndicator.tsx:25, app.tsx:21

□ 8.5 Mettre à jour docs/development/api-endpoints.md (périmé depuis 2025-04-11)
```

---

### 📊 TABLEAU RÉCAPITULATIF

| Phase | # Actions | Dépend de | Temps estimé | Parallélisable |
|-------|-----------|-----------|-------------|----------------|
| **P0** Environnement | 2 | Rien | 30 min | Non |
| **P1** Build | 13 | P0 | 2-3h | Par blocs |
| **P2** Tests | 4 | P1 | 30 min | Non |
| **P3** Docker | 5 | P1 | 1h | Partiellement |
| **P4** Sécurité | 6 | P1 | 2-3h | Oui (après P1) |
| **P5** Intégrité | 8 | P1 | 4-6h | Oui (après P1) |
| **P6** Nettoyage | 6 | P1 | 1-2h | Oui (après P1) |
| **P7** Fonctionnel | 10 | P1 | 6-10h | Oui (après P1) |
| **P8** CI/CD | 5 | P3 | 1-2h | Oui (après P3) |
| **TOTAL** | **59** | | **~22-32h** | |

**Ordre d'exécution recommandé** : P0 → P1 → P2 (valider build+tests) → lancer P3, P4, P5, P6, P7 en parallèle → P8

---

*Document généré le 26 Avril 2026 — 6 passes d'audit + plan de résolution*
*Total issues documentées : ~560+*
*Total actions correctives : 59*
