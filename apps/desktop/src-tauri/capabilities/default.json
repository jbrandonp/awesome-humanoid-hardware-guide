{
  "$schema": "../../node_modules/@tauri-apps/cli/config.schema.json",
  "identifier": "default",
  "description": "Default capabilities allowing no network access other than required.",
  "windows": ["*"],
  "permissions": ["core:default"]
}
