--- apps/mobile/src/components/PrescriptionForm.tsx
+++ apps/mobile/src/components/PrescriptionForm.tsx
@@ -8,6 +8,7 @@
   FlatList,
   StyleSheet,
   ScrollView,
+  AccessibilityInfo,
 } from 'react-native';
 import {
   ClinicalProtocolManager,
@@ -82,6 +83,8 @@
         ) : null}
       </View>
       <TouchableOpacity
+        accessible={true}
+        accessibilityRole="button"
+        accessibilityLabel={`Supprimer ${item.name}`}
         style={styles.removeButton}
         onPress={() => handleRemoveMedication(item.id)}
       >
@@ -99,6 +102,8 @@
         <ScrollView horizontal showsHorizontalScrollIndicator={false}>
           {protocols.map((protocol) => (
             <TouchableOpacity
+              accessible={true}
+              accessibilityRole="button"
               key={protocol.id}
               style={styles.protocolBadge}
               onPress={() => handleApplyProtocol(protocol)}
@@ -113,22 +118,34 @@
       <View style={styles.section}>
         <Text style={styles.sectionTitle}>Ajout Manuel</Text>
         <TextInput
+          accessible={true}
+          accessibilityLabel="Nom du médicament"
           style={styles.input}
           placeholder="Nom du médicament"
           value={medName}
           onChangeText={setMedName}
         />
         <TextInput
+          accessible={true}
+          accessibilityLabel="Dosage du médicament"
           style={styles.input}
           placeholder="Dosage (ex: 500mg)"
           value={medDosage}
           onChangeText={setMedDosage}
         />
         <TextInput
+          accessible={true}
+          accessibilityLabel="Instructions optionnelles pour le patient"
           style={styles.input}
           placeholder="Instructions (Optionnel)"
           value={medInstructions}
           onChangeText={setMedInstructions}
         />
-        <TouchableOpacity style={styles.addButton} onPress={handleAddMedication}>
+        <TouchableOpacity
+          accessible={true}
+          accessibilityRole="button"
+          style={styles.addButton}
+          onPress={handleAddMedication}
+        >
           <Text style={styles.addButtonText}>Ajouter Médicament</Text>
         </TouchableOpacity>
@@ -155,12 +172,17 @@
           <Text style={styles.sectionTitle}>Sauvegarder en tant que Protocole</Text>
           <View style={styles.createProtocolRow}>
             <TextInput
+              accessible={true}
+              accessibilityLabel="Nom du nouveau protocole"
               style={[styles.input, styles.createProtocolInput]}
               placeholder="Nom du nouveau protocole"
               value={protocolName}
               onChangeText={setProtocolName}
             />
             <TouchableOpacity
+              accessible={true}
+              accessibilityRole="button"
               style={styles.createProtocolButton}
               onPress={handleCreateProtocol}
             >
