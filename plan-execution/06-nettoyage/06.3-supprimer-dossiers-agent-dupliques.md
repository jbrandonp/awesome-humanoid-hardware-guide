# 06.3 — Supprimer les 28 dossiers .agent/ dupliqués

**Réf** : `260426.md:1137-1142`  
**Priorité** : 🟡 P6 — NETTOYAGE MAJEUR  
**Durée estimée** : 5 min  
**Dépend de** : P1

---

## Contexte

28 dossiers d'agents AI (`.adal/`, `.agents/`, `.augment/`, etc.) sont dupliqués. Seuls `skills/` (canonique) et `.opencode/` (utilisé) doivent être conservés. Ces dossiers contiennent des skills dupliqués, alourdissent le repo, et créent de la confusion.

---

## Dossiers à supprimer

```
.adal/       .agents/     .augment/    .bob/        .claude/
.codebuddy/  .commandcode/ .continue/  .cortex/     .crush/
.factory/    .goose/      .iflow/      .junie/      .kilocode/
.kiro/       .kode/       .mcpjam/     .mux/        .neovate/
.openhands/  .pi/         .pochi/      .qoder/      .qwen/
.roo/        .trae/       .vibe/       .windsurf/   .zencoder/
```

---

## Action

```bash
rm -rf .adal/ .agents/ .augment/ .bob/ .claude/ \
       .codebuddy/ .commandcode/ .continue/ .cortex/ .crush/ \
       .factory/ .goose/ .iflow/ .junie/ .kilocode/ \
       .kiro/ .kode/ .mcpjam/ .mux/ .neovate/ \
       .openhands/ .pi/ .pochi/ .qoder/ .qwen/ \
       .roo/ .trae/ .vibe/ .windsurf/ .zencoder/
```

---

## À conserver

```
skills/       → dossier canonique des skills
.opencode/    → utilisé par l'outil opencode
```

---

## Vérification

```bash
ls -d .*/ 2>/dev/null | grep -v ".git/" | grep -v ".opencode/" | grep -v ".husky/"
# Ne doit rien retourner sauf .git/, .opencode/, .husky/
```

---

## Risques

- **Faible** : Dossiers dupliqués, pas de code unique perdu
- Si l'utilisateur utilise un de ces agents → backup dans une branche `archive/agent-skills`
