# 08.5 — Mettre à jour docs/development/api-endpoints.md

**Réf** : `260426.md:770-771` + `260426.md:1226`  
**Priorité** : 🟢 P8 — DOCUMENTATION  
**Durée estimée** : 15 min  
**Dépend de** : P4, P5, P7

---

## Contexte

`docs/development/api-endpoints.md` date du **11 avril 2025** (plus d'un an). La structure de l'API a considérablement évolué depuis (nouveaux endpoints, guards ajoutés, etc.).

---

## Fichier concerné

```
docs/development/api-endpoints.md
```

---

## Action

1. Lister tous les contrôleurs actuels :
```bash
find apps/api/src -name "*.controller.ts" -exec basename {} \;
```

2. Pour chaque contrôleur, lister les endpoints (`@Get`, `@Post`, `@Put`, `@Delete`, `@Patch`)

3. Mettre à jour le document avec :
   - Endpoint path
   - Méthode HTTP
   - Auth requise (JWT, API Key, publique)
   - Body schema (si applicable)
   - Response schema

---

## Vérification

Le document doit refléter l'état actuel du code post-corrections (P4-P5-P7).

---

## Risques

- **Faible** : Documentation seulement
- Automatiser avec Swagger/OpenAPI à l'avenir
