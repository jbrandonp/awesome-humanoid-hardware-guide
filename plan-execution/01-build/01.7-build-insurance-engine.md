# 01.7 — Build librairie insurance-engine

**Réf** : `260426.md:948`  
**Priorité** : 🔴 P1 — SÉQUENTIEL (dépend de 01.6)  
**Durée estimée** : 3 min  
**Dépend de** : 01.6

---

## Contexte

La librairie `insurance-engine` est une dépendance de l'API. Elle doit être buildée avant l'API.

---

## Fichier concerné

```
libs/insurance-engine/
```

---

## Action

```bash
npx nx build insurance-engine
```

---

## Vérification

```bash
ls libs/insurance-engine/dist/index.js && echo "OK" || echo "FAIL"
```

---

## Risques

- **Faible** : Librairie indépendante
- Si bug `Dinero.minimum()` (LB6, `260426.md:664`) → voir action 07.9
