# 02.3 — Lancer les tests API

**Réf** : `260426.md:1004`  
**Priorité** : 🔴 P2 — VALIDATION  
**Durée estimée** : 10 min  
**Dépend de** : P1, 02.2

---

## Contexte

Lancer la suite de tests de l'API NestJS pour identifier les tests cassés après les corrections P1.

---

## Action

```bash
npx nx test api
```

---

## Problèmes anticipés

D'après l'audit (`260426.md:232-275`) :

| Test | Bug | Impact |
|------|-----|--------|
| `*.test.ts` (×3) | `.overrideProvider('PrismaService')` string vs classe (T1) | Tests utilisent la vraie DB |
| `patient.test.ts:46` | `authToken` non valide (T2) | 401 → faux négatifs |
| `patient.test.ts:110-116` | Test attend 200 mais mock retourne null (T3) | Test cassé |
| `resilience.test.ts:76-88` | Rate limiting vérification trop permissive (T4) | Faux positifs |

---

## Vérification

Identifier tous les tests échoués et les corriger avant de passer à P3.

---

## Risques

- **Élevé** : Beaucoup de tests ont des problèmes structurels (T1-T30)
- Prioriser la correction des tests qui bloquent la CI
